apiVersion: route.openshift.io/v1
kind: Route
metadata: 
  labels: 
    app.kubernetes.io/instance: jenkins-test
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: jenkins-test 
  name: jenkins-test
spec:
  port:
    targetPort: 8081 
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: jenkins-test
    weight: 100 
